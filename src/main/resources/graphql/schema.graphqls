type Book {
    id: ID!
    title: String
    pages: Int
    author: Author
}

type Author {
    id: ID!
    name: String
    nationality: String
    age: Int
}

type Response {
    message: String
    data: Object
}

type BookList {
    books: [Book]
}

type Query {
    books: [Book]
    getBookById(id: ID!): Book
    search(title: String = "", pages: Int = 0, authorName: String = "", authorNationality: String = ""): [Book]
}

type Mutation {
    updateBook(id: ID!, bookInput: BookInput): Book
    deleteBookById(id: ID!): Response
}

input BookInput {
    title: String
    pages: Int
}

union Object = Book | BookList